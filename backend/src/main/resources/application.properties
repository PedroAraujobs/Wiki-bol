spring.application.name=AP6
app.frontend-url=${APP_FRONTEND_URL:http://localhost:5173}

spring.datasource.url=${SUPABASE_DATABASE_URL:jdbc:postgresql://aws-1-us-east-1.pooler.supabase.com:5432/postgres?sslmode=require}
spring.datasource.username=${SUPABASE_DATABASE_USERNAME:postgres.ekmpdqbrldlfpzgnkndi}
spring.datasource.password=${SUPABASE_DATABASE_PASSWORD}
spring.datasource.driver-class-name=org.postgresql.Driver

spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=${SPRING_JPA_SHOW_SQL:false}
spring.jpa.properties.hibernate.format_sql=${SPRING_JPA_FORMAT_SQL:false}

spring.servlet.multipart.max-file-size=5MB
spring.servlet.multipart.max-request-size=5MB

spring.security.oauth2.client.registration.google.client-id=${GOOGLE_CLIENT_ID}
spring.security.oauth2.client.registration.google.client-secret=${GOOGLE_CLIENT_SECRET}
spring.security.oauth2.client.registration.google.scope=openid,email,profile

supabase.url=${SUPABASE_URL:https://ekmpdqbrldlfpzgnkndi.supabase.co}
supabase.storage.bucket=${SUPABASE_STORAGE_BUCKET:wiki-images}
supabase.service-role-key=${SUPABASE_SERVICE_ROLE_KEY}
